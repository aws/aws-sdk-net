{
    "core": {
        "changeLogMessages": [
            "Update `SSOTokenManager` to propagate cancellation token to `GetSsoTokenAsync` flow"
        ],
        "type": "patch",
        "updateMinimum": true
    }
}