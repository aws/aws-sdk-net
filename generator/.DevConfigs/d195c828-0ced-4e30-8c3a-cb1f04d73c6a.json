{
  "core": {
    "updateMinimum": true,
    "type": "patch",
    "changeLogMessages": [
      "All JSON based services will send sub-second precise timestamps in the body if specified on the request."
    ]
  }
}